{
  "backgroundSync": {
    "description": "Keep a previously-paired BLE device connected while Safari is backgrounded. No iOS notification is shown — the GATT link is simply maintained by the beacio companion app so the web page sees a live connection when the user returns. Lives under window.beacioIOS.backgroundSync.requestBackgroundConnection({ deviceId }).",
    "example": "if (!('beacioIOS' in window)) throw new Error('Requires beacio companion app');\n\nconst device = await navigator.bluetooth.requestDevice({\n  filters: [{ services: ['heart_rate'] }]\n});\nawait device.gatt.connect();\n\nconst reg = await window.beacioIOS.backgroundSync.requestBackgroundConnection({\n  deviceId: device.id\n});\n// Connection now persists across Safari backgrounding / tab suspension.\n// Tear down with: await reg.unregister();",
    "requires_app_store": true
  },
  "notifications": {
    "description": "Deliver an iOS Lock Screen / Dynamic Island notification when a GATT characteristic value changes in the background. Template title/body/url support placeholders like {{deviceName}}, {{value.utf8}}, {{value.int16be}}, {{timestamp}}. Condition gates delivery via decoder + operator + threshold. Cooldown throttles OS notification rate (minimum 5 s).",
    "example": "if (!('beacioIOS' in window)) throw new Error('Requires beacio companion app');\n\nconst status = await window.beacioIOS.backgroundSync.requestPermission();\nif (status !== 'granted') throw new Error('Notification permission denied');\n\nawait window.beacioIOS.backgroundSync.registerCharacteristicNotifications({\n  deviceId: device.id,\n  serviceUUID: 'heart_rate',\n  characteristicUUID: 'heart_rate_measurement',\n  condition: { decode: 'uint8', operator: 'gt', threshold: 150 },\n  template: {\n    title: 'High heart rate',\n    body: '{{deviceName}} is at {{value.utf8}} bpm',\n    url: 'https://example.com/alert'\n  },\n  cooldownSeconds: 30,\n  replyAction: { actionTitle: 'Ack', placeholder: 'Reply…' }\n});",
    "requires_app_store": true
  },
  "liveActivity": {
    "description": "Live Activities (Lock Screen / Dynamic Island) are managed automatically by the beacio companion app in IPC-relay mode — they reflect active background-sync registrations (keep-alive connections, characteristic alerts, beacon scans). There is no direct JS opt-in or opt-out today; the page registers background-sync intents and ActivityKit state follows. Standalone mode does not expose Live Activities.",
    "example": "// No direct API. Create any backgroundSync registration while the companion app\n// is running in IPC-relay mode, and the Live Activity appears automatically:\nif (!('beacioIOS' in window)) throw new Error('Requires beacio companion app');\n\nawait window.beacioIOS.backgroundSync.requestBackgroundConnection({ deviceId: device.id });\n// ➜ Lock Screen / Dynamic Island Live Activity starts reflecting this registration.",
    "requires_app_store": true
  },
  "beacons": {
    "description": "Fire an iOS notification when a BLE advertisement matching registered service-UUID filters is seen while Safari is backgrounded. iOS hardware-filters on service UUIDs; namePrefix is applied in software. Delivery is optimized for catch-up rather than guaranteed real-time while backgrounded. cooldownSeconds dedups notifications per registration.",
    "example": "if (!('beacioIOS' in window)) throw new Error('Requires beacio companion app');\n\nawait window.beacioIOS.backgroundSync.registerBeaconScanning({\n  filters: [{ services: ['battery_service'], namePrefix: 'Acme' }],\n  cooldownSeconds: 30,\n  template: {\n    title: 'Acme beacon in range',\n    body: 'at {{timestamp}}',\n    url: 'https://example.com/beacon'\n  }\n});",
    "requires_app_store": true
  },
  "peripheral": {
    "description": "Turn the iPhone into a GATT peripheral advertising a custom service. Scanning centrals (other phones, laptops, sensor tags) discover and connect to it like any dedicated device. No other browser exposes GATT-server mode. Access via window.beacioIOS.peripheral — addService(), startAdvertising(), stopAdvertising(), and events onwriterequest / onsubscriptionchange / onconnectionstatechange.",
    "example": "if (!('beacioIOS' in window)) throw new Error('Requires beacio companion app');\n\nconst CHAT_SERVICE = '12345678-1234-5678-1234-56789abcdef0';\nconst CHAT_CHAR    = '12345678-1234-5678-1234-56789abcdef1';\n\nawait window.beacioIOS.peripheral.addService({\n  uuid: CHAT_SERVICE,\n  characteristics: [{\n    uuid: CHAT_CHAR,\n    properties: ['read', 'write', 'notify'],\n    value: new TextEncoder().encode('hello')\n  }]\n});\nwindow.beacioIOS.peripheral.onwriterequest = (ev) => {\n  console.log('central wrote', new TextDecoder().decode(ev.value));\n};\nawait window.beacioIOS.peripheral.startAdvertising({\n  localName: 'beacio Chat',\n  serviceUUIDs: [CHAT_SERVICE]\n});",
    "requires_app_store": true
  },
  "whiteLabel": {
    "description": "The beacio Safari Web Extension and companion app can be re-skinned and shipped under a hardware manufacturer's own App Store identity. This is a commercial arrangement covering bundle identifiers, marketing metadata, App Store Connect ownership, and IPC-relay signing — not a runtime JS API. Contact via the beacio.com 'Talk to us' form or see the [pricing page](https://beacio.com/pricing.html#enterprise).",
    "example": "// No JS surface — white-label is a build-time / App-Store-listing concern.\n// Development flow (handled by beacio engineering):\n//   1. Fork the companion app + extension with the manufacturer's bundle IDs.\n//   2. Swap assets (icons, onboarding copy, Live Activity tokens).\n//   3. Submit to App Store Connect under the manufacturer's team.\n// Runtime feature-detection stays identical: `'beacioIOS' in window`.",
    "requires_app_store": true
  }
}
